import { style } from '@vanilla-extract/css';

export const choiceListClass = style({
  display: 'flex',
  flexDirection: 'column',
  gap: 12,
  listStyle: 'none',
  margin: 0,
  padding: '0 40px',
});
