import { style } from '@vanilla-extract/css';

export const rubyClass = style({
  alignItems: 'center',
  display: 'flex',
  flexDirection: 'column',
});

export const rtClass = style({
  fontSize: '.75rem',
});
